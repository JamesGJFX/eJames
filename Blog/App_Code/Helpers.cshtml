@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, 
    IList<Blog.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">
        @Blog.ActionLinkExtensions.TagLink(htmlHelper, tag)
    </div>
  }
}